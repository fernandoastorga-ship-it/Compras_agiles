services:
  - type: web
    name: compras-agiles
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: APP_PASSWORD
        sync: false
      - key: MAX_FILE_BYTES
        value: "10485760"
      - key: MAX_PURCHASE_FILES_BYTES
        value: "31457280"
